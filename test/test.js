let mocha = require('mocha'),
    chai = require('chai'),
    expect = require('chai').expect,
    seneca = require('seneca');


describe('hackerNews service', function() {
    this.timeout(10000);

    // it('should return "Hello " + what I passed in', function(fin) {
    //
    //     seneca.act({
    //         service: 'hello',
    //         params: 'Bob'
    //     }, function(ignore, data) {
    //         expect(data.result).to.equal('Hello Bob');
    //         fin();
    //     })
    // });
});

